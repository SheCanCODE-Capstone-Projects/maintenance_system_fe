"use client";

import { useState } from "react";
import { CheckCircle2, Eye, FilePenLine, XCircle } from "lucide-react";

const initialQueue = [["SI", "Solange Ineza", "Technician", "Painting · Gitarama, Nyamirambo · submitted Jul 14, 2025", "solange.i@gmail.com · +250 788 600 003 · 3 docs uploaded"], ["IH", "Innocent Habimana", "Technician", "Plumbing · Gasabo, Kimihurura · submitted Jul 15, 2025", "innocent.h@gmail.com · +250 788 600 004 · 2 docs uploaded"], ["CL", "CleanPro Ltd", "Company", "General Maintenance · Kicukiro, Gatenga · submitted Jul 16, 2025", "info@cleanpro.rw · +250 789 300 300 · 1 doc uploaded"]];
export default function Page() { const [queue, setQueue] = useState(initialQueue); const [message, setMessage] = useState(""); const resolve = (name: string, result: string) => { setQueue(q => q.filter(item => item[1] !== name)); setMessage(`${name} was ${result}.`); }; return <main className="min-h-screen bg-[#f5f7f6] p-7 lg:p-10"><header><h1 className="font-heading text-2xl font-extrabold text-[#132d2a]">Verification Queue</h1><p className="mt-1 text-sm text-slate-500">{queue.length} pending review</p></header>{message && <p className="mt-4 rounded-lg bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{message}</p>}<div className="mt-5 space-y-3">{queue.map(([initials, name, type, details, contact]) => <article key={name} className="flex flex-col gap-4 rounded-xl border border-slate-200 bg-white p-4 shadow-sm xl:flex-row xl:items-center"><span className={`grid h-9 w-9 shrink-0 place-items-center rounded-full text-xs font-bold ${type === "Company" ? "bg-blue-50 text-blue-600" : "bg-[#fff0eb] text-[#ff6224]"}`}>{initials}</span><div className="flex-1"><div className="flex items-center gap-2"><h2 className="text-sm font-bold text-[#132d2a]">{name}</h2><span className="rounded-full bg-orange-50 px-2 py-0.5 text-[10px] font-bold text-[#ff6224]">{type}</span><span className="rounded-full bg-amber-100 px-2 py-0.5 text-[10px] font-bold text-amber-700">Pending</span></div><p className="mt-1 text-xs text-slate-500">{details}</p><p className="mt-1 text-xs text-slate-600">{contact}</p></div><div className="flex flex-wrap gap-2"><button className="inline-flex items-center gap-1 rounded-lg bg-blue-50 px-3 py-2 text-xs font-semibold text-blue-600"><Eye className="h-3.5 w-3.5" />Review</button><button className="inline-flex items-center gap-1 rounded-lg bg-orange-50 px-3 py-2 text-xs font-semibold text-[#ff6224]"><FilePenLine className="h-3.5 w-3.5" />Edit</button><button onClick={() => resolve(name, "rejected")} className="inline-flex items-center gap-1 rounded-lg border border-rose-100 px-3 py-2 text-xs font-semibold text-rose-600"><XCircle className="h-3.5 w-3.5" />Reject</button><button onClick={() => resolve(name, type === "Company" ? "accredited" : "verified")} className="inline-flex items-center gap-1 rounded-lg bg-[#0f7651] px-3 py-2 text-xs font-semibold text-white"><CheckCircle2 className="h-3.5 w-3.5" />{type === "Company" ? "Accredit" : "Verify"}</button></div></article>)}{!queue.length && <div className="rounded-xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500">All caught up — no pending verifications.</div>}</div></main>; }
