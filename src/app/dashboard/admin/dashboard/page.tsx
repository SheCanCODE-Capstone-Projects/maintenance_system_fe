import Link from "next/link";
import { AlertTriangle, CheckCircle2, CircleDollarSign, ShieldAlert, Users } from "lucide-react";
import AdminPageHeader from "@/components/admin/AdminPageHeader";

const stats = [["Total users", "1,284", "+12 today", Users, "text-primary"], ["Pending verification", "3", "Awaiting review", AlertTriangle, "text-amber-600"], ["Payments", "214", "RWF 6.2M total", CircleDollarSign, "text-blue-600"], ["Blocked accounts", "7", "2 pending review", ShieldAlert, "text-red-600"]] as const;
const activity = [["Patrick N. approved as verified technician", "Plumbing · by Admin", "Jul 9, 09:12", "success"], ["Eric N. subscription payment received", "RWF 48,000 · Annual plan", "Jul 9, 08:47", "payment"], ["Grace T. application flagged for review", "Mismatched ID document", "Jul 8, 17:23", "warning"], ["Account blocked after 3 failed login attempts", "Customer · auto-blocked", "Jul 8, 14:05", "danger"]] as const;
const dots = { success: "bg-emerald-50 text-emerald-600", payment: "bg-blue-50 text-blue-600", warning: "bg-amber-50 text-amber-600", danger: "bg-red-50 text-red-600" };
export default function DashboardPage() { return <div className="mx-auto max-w-7xl p-4 sm:p-6 lg:p-7"><AdminPageHeader title="Admin Dashboard" description="Platform overview" /><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{stats.map(([label, value, detail, Icon, color]) => <section key={label} className="rounded-xl border border-slate-200 bg-white p-4 shadow-sm"><Icon size={18} className={color} /><p className="mt-3 text-xs text-slate-500">{label}</p><p className="font-heading text-2xl font-extrabold text-secondary">{value}</p><p className={`mt-1 text-xs ${color}`}>{detail}</p></section>)}</div><div className="mt-5 flex flex-wrap items-center justify-between gap-3 rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-700"><span className="flex items-center gap-2 font-medium"><AlertTriangle size={16} />3 pending verifications</span><Link href="/dashboard/admin/verification-queue" className="rounded-lg bg-primary px-3 py-2 text-xs font-bold text-white hover:bg-orange-600">Review now</Link></div><h2 className="mt-6 font-heading text-base font-bold text-secondary">Recent activity</h2><section className="mt-3 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">{activity.map(([title, subtitle, date, status]) => <div key={title} className="flex gap-3 border-b border-slate-100 p-4 last:border-0"><span className={`grid h-8 w-8 shrink-0 place-items-center rounded-lg ${dots[status]}`}><CheckCircle2 size={15} /></span><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-secondary">{title}</p><p className="mt-0.5 text-xs text-slate-500">{subtitle}</p></div><time className="hidden whitespace-nowrap text-xs text-slate-400 sm:block">{date}</time></div>)}</section></div>; }
